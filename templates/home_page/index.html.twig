{% extends 'base.html.twig' %}

{% block title %}Test JR{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .app-wrapper {display: flex; flex-direction: column; justify-content: center;}
    .app-wrapper h1, .app-wrapper h2 {text-align: center;}
</style>

<div class="example-wrapper">
  <div class="app-wrapper">
    <h1 class="mb-5">Test Julien Rémond</h1>

    {# le compteur est bien relié à la base de données #}
    <h2 class="mb-5">Current count : {{ count.count }}</h2>

    {# je n'ai pas su passer la fonction d'incrément au bouton #}
    <button type="button" class="btn btn-success">Increment</button>
  </div>   
</div>
{% endblock %}
